App.namespace("views.invoicing");
App.views.invoicing.InvoiceContainer = (function($) {
  "use strict";

  var inst = {};

  inst.init = function() {
    App.views.common.Common.init();
  };

  return inst;
}(jQuery));
