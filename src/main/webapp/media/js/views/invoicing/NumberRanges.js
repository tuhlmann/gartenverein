App.namespace("views.invoicing");
App.views.invoicing.NumberRanges = (function($) {
  "use strict";

  var inst = {};

  inst.init = function() {
    App.views.common.Common.init();
  };

  return inst;
}(jQuery));
