App.namespace("views.invoicing");
App.views.invoicing.Invoices = (function($) {
  "use strict";

  var inst = {};

  inst.init = function() {
    App.views.common.Common.init();
  };

  return inst;
}(jQuery));
