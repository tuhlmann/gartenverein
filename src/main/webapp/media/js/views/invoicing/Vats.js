App.namespace("views.invoicing");
App.views.invoicing.Vats = (function($) {
  "use strict";

  var inst = {};

  inst.init = function() {
    App.views.common.Common.init();
  };

  return inst;
}(jQuery));
